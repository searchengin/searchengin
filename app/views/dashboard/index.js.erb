$('#url_listing').append('<%= j render @urls %>');
<% if @urls.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @urls %>');
<% else %>
  $(window).off('scroll');
  $('.loader').remove();
<% end %>
